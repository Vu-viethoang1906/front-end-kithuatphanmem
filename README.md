# front-end-kithuatphanmem
lưu trữ mã nguồn front-end
- Các lệnh tải chạy mã:
npm install
npm install axios
npm install react-router-dom
npm install -D tailwindcss@3.4.4 postcss autoprefixer
npm install socket.io-client
- lệnh chạy:
npm start
- lệnh chạy test:
npm run test
npm run test:ci
